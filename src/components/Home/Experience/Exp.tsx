import React from "react";
import Container from "@/components/ui/container/Container";

type Props = {};

const Exp = (props: Props) => {
  return <Container headingName={"Experience"}></Container>;
};

export default Exp;
